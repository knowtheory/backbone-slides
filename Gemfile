source 'https://rubygems.org'
dependencies = %w(omniauth omniauth-twitter dm-core dm-postgres-adapter dm-migrations dm-types dm-serializer json)
dependencies.each { |dependency| gem dependency }
gem 'sinatra', :require => 'sinatra/base'
